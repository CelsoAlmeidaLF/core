@model IEnumerable<ENT.eFuncionarios>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center vh-100">

    <div class="card">
        <!--div class="card-header"></div>-->
        <div class="card-body h-50">
            <h3>FORMULARIO DE CADASTRO</h3>
            <p>Meu formulario</p>
        </div>
    </div>
    <br />
    <div class="card">
        <div class="card-header text-left">
            <h3> TABLE </h3>
        </div>
        <div class="card-body">

            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Nome</th>
                        <th scope="col">Depto</th>
                        <th scope="col">Função</th>
                        <th scope="col">Salario</th>
                    </tr>
                </thead>
                <tbody>

                    @*@foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.eIdFunc)</td>
                            <td>@Html.DisplayFor(modelItem => item.eNome)</td>
                            <td>@Html.DisplayFor(modelItem => item.eDepto)</td>
                            <td>@Html.DisplayFor(modelItem => item.eFuncao)</td>
                            <td>@Html.DisplayFor(modelItem => item.eSalario)</td>
                    </tr>
                    }*@
                    </tbody>
            </table>

        </div>
    </div>
</div>
